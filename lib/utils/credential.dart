class Credential{

  String title;
  String username;
  String password;
  String notes;

  Credential(this.title,this.username,this.password,this.notes);

}
/*
class Credentials
    title = Encrypted String
    username = Encrypted String
    password = Encrypted String
    notes = Encrypted String
    link = Encrypted String
 */